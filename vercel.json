{
  "git": {
    "ignoreCommand": "if [[ \"$VERCEL_GIT_COMMIT_REF\" == \"main\" || \"$VERCEL_GIT_COMMIT_REF\" == \"chore/dev\" || \"$VERCEL_GIT_COMMIT_REF\" == \"chore/pre-dev\" ]]; then exit 1; else exit 0; fi"
  }
}
